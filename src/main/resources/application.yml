spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gnb_local
    username: root
    password: dnsgkdtlf11
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    org:
      springframework: DEBUG